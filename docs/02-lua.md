# Lua

Lua no servidor cria regras de jogo. Lua no client controla UI e eventos locais. Use `local`, valide `nil`, retorne cedo em entrada invalida e nunca confie em dados vindos do client.

Arquivos importantes: `data/scripts/`, `data/lib/`, `modules/*/*.lua`, `modules/corelib/` e `modules/gamelib/`.
